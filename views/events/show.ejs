<!DOCTYPE html>
<html lang="en">
  <%- include("../partials/head.ejs",{title: "Show Event"}) %>
  <body>
    <%- include("../partials/header.ejs") %>
    <div class="container">
      <div class="col-md-3"><%=event.eventName%></div>
      <div class="row">
        <div class="col-md-3" id="button">
          <a href="/event/edit/<%=event._id %>">Edit Event</a>
        </div>
        <div class="col-md-3" id="button">
          <a href="/runner/<%=event._id %>/new">Add Runner</a>
        </div>
        <div class="col-md-2">
          <form
            id="delete"
            action="/event/<%=event._id%>?_method=DELETE"
            method="POST"
          >
            <div id="button"><button>Delete Event</button></div>
          </form>
        </div>
      </div>
    </div>
    <div class="container">
      <% for( let i = 0; i < runners.length; i++ ) { %>
      <div class="row" id="event-item">
        <div class="col-md-4">
          <a href="/runner/<%=event._id%>/<%=runners[i]._id%>"
            ><%=runners[i].firstName%> <%=runners[i].lastName%></a
          >
        </div>
        <div class="col-md-4"><%=runners[i].age%></div>
        <div class="col-md-4"><%=runners[i].pace%></div>
      </div>
      <% } %>
    </div>
  </body>
</html>
